const React = require('react');
const RoundSelector = require('./../RoundPicker');

class Swatches extends React.Component {
  render() {
    return (
      <RoundSelector />
    );
  }
}

module.exports = Swatches;
